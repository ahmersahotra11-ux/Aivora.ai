# Aivora.ai - Starter
Aivora.ai — AI Chatbot by Infinity Duo ♾️

## Quick start
1. Copy `.env.example` -> `.env.local` and set `POSTGRES_URL` and `OPENAI_API_KEY`.
2. Run `npm install` then `npm run dev`.
3. Use Neon SQL to create `messages` table (see sql/create_tables.sql).
4. Push to GitHub and connect to Vercel.
