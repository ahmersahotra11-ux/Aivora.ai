export default function Message({m}) {
  return (
    <div style={{padding:8, borderRadius:6, background: m.sender==='user' ? '#0f62fe' : '#198038', color:'#fff'}}>
      <strong>{m.sender}:</strong> {m.text}
    </div>
  )
}
